# クレジットカード辞典 サイト仕様書

このサイトの目的・編集方針・技術仕様をまとめた文書。
記事を追加するとき、機能を追加するとき、方針に迷ったときはまずここを読む。

最終更新: 2026-08-30

---

## 1. サイト定義

| 項目 | 内容 |
| :--- | :--- |
| サイト名 | クレジットカード辞典 |
| ドメイン | https://cc-jiten.com |
| 運営者 | いととん(個人・単独運営) |
| ホスティング | Cloudflare(GitHubへのpushで自動ビルド・デプロイ) |
| リポジトリ | iToTon/credit-card-affiliate-site |
| 収益モデル | アフィリエイト(現在 A8.net / AccessTrade) |

**読者**: クレジットカードについて具体的な疑問を持って検索してきた人。とくに、大手比較サイトの
ランキング記事では答えが得られない、条件が細かい・状況が特殊な読者。

**サイトの役割**: カードを網羅的に比較するデータベースではない。
特定の状況(法人設立直後、審査に不安がある、系列カードを検討中など)に対して、
一段深く踏み込んだ説明を提供する。

---

## 2. 編集方針

### 2.1 市場の3層構造

競合分析の結論として、この市場は3つの層に分かれている。**どの層に向けて書くかが、記事の成否をほぼ決める。**

| 層 | 領域 | 占有している競合 | 判断 |
| :--- | :--- | :--- | :--- |
| 第1層 | 「〜とは」「仕組み」などの解説 | カード発行会社・銀行(三菱UFJ、JCB、三井住友、セゾン等) | **新規に書かない** |
| 第2層 | 「おすすめ」「ランキング」「比較」 | 大手比較メディア(価格.com、マネ会、みんかぶ、マイナビ等) | **新規に書かない** |
| 第3層 | 特化・ニッチ | 中堅メディア、広告主自身、個人サイト | **ここに集中する** |

第1層は当事者による一次情報でありYMYL領域でもあるため、個人運営の新規ドメインでは覆せない。
第2層は掲載枚数(価格.com 2,350枚など)そのものが参入障壁になっている。

第3層も無競争ではない。ただし「層が薄い」ため、掘り下げれば差をつけられる。

### 2.2 新規記事のチェックリスト

新しい記事を提案・作成する前に、以下を確認する。**1つでもNoがあれば、そのテーマは書かない。**

- [ ] カード発行会社が自社サイトで書けない/書かないテーマか
      (他社との比較、審査に不安がある層向けの選択肢、そもそもクレジットカードでない商材など)
- [ ] 大手比較メディアが網羅性の中で1行触れる程度にとどまるテーマか
- [ ] 収益導線(アフィリエイト案件)を張れるか、または第3層の収益記事へ内部リンクで送客できるか
- [ ] 検索する人の状況と意図が具体的に想像できるか

### 2.3 優先して書く領域

1. **法人・個人事業主向け** — 最有力。発行会社は自社カードの話しかせず、大手は個人向けが主戦場。
   協同組合系のガソリンカードは「クレジットカードではない」ため比較データベースに載らない。
   業種別・設立年数別・車両台数別など、要件を細かく分けた記事に展開余地がある。
2. **系列・地域カードのレビュー** — 小田急ポイントカード、コスモ・ザ・カード・オーパスなど。
   大手が網羅性の中で軽く触れるだけになりやすい。
3. **一次体験** — 実際に申し込んだ記録、届いたカード、審査にかかった日数。
   資本で代替できない唯一の資産。書ける場合は最優先。

### 2.4 既存記事の扱い

公開69本のうち56本は第1層・第2層に属し、収益導線を持たない。**これらは削除もリライトもしない。**

理由: 流入は見込めないが、第3層の収益記事へ内部リンクを流す受け皿として機能し、
サイト全体のテーマの一貫性にも寄与している。

リライトの優先順位は、Search Console に表示回数が蓄積されてから判断する(→ 6.2)。

---

## 3. コンテンツ仕様

### 3.1 カテゴリー

`src/site.config.ts` の `categories` が唯一の定義。ここに無い slug は
`src/content.config.ts` の zod enum で弾かれ、**ビルドが失敗する**(タイプミスの検出を意図した設計)。

| slug | 名称 | 属する層 |
| :--- | :--- | :--- |
| `how-to-choose` | 選び方・比較 | 第2層 |
| `screening` | 審査・発行 | 第1層 |
| `how-it-works` | 使い方の基礎知識 | 第1層 |
| `points` | ポイント・特典活用 | 第1層 |
| `business` | 法人・個人事業主向け | **第3層** |
| `everyday-payments` | 支払い活用術 | **第3層** |
| `trouble` | トラブル対応・セキュリティ | 第1層 |
| `card-reviews` | 個別カードレビュー | **第3層** |
| `life-stage` | 属性別ガイド | 第2層 |

カテゴリーを追加する場合は `site.config.ts` に `slug` / `name` / `description` / `accent` / `tint` の
5項目すべてを定義する。`accent`・`tint` はアイコンとアイキャッチの配色に使われる。

### 3.2 フロントマター

```yaml
---
title: "記事タイトル"           # 必須
description: "検索結果に出る説明文"  # 必須
category: "card-reviews"        # 必須。上表の slug のみ
pubDate: 2026-08-28             # 必須
updatedDate: 2026-09-15         # 任意。内容を更新したとき
draft: false                    # 任意(既定 false)。true で一覧・サイトマップから除外
ogImage: "/path.png"            # 任意。未指定なら自動生成されたOGP画像を使う
---
```

### 3.3 記事の構成

`src/content/articles/*.mdx` に配置。ファイル名がそのまま URL の slug になる。

標準的な構成:

1. 導入(誰向けの記事か、1〜2段落)
2. `## 見出し` で本題(必要に応じて `### 小見出し`)
3. `## こんな人におすすめ` / `## 申し込み前に確認しておきたいこと` など
4. `## まとめ`

見出しは `##` から始める。`#` は記事タイトルとしてレイアウトが自動で描画するため本文では使わない。

### 3.4 内部リンク

Markdown 記法で書く。生の `<a>` タグは使わない。

```markdown
[クレジットカードの選び方](/articles/how-to-choose-credit-card/)の記事もあわせてご参照ください。
```

末尾のスラッシュを含めること。下書き(`draft: true`)の記事へはリンクしない(404になる)。

記事下部の「関連記事」は自動生成されるため、手動で一覧を作る必要はない。

### 3.5 アフィリエイトリンク

必ず `AffiliateLink` コンポーネントを使う。生の `<a>` タグは使わない。

```mdx
import AffiliateLink from "../../components/AffiliateLink.astro";

<AffiliateLink href="https://...">
  カードの詳細を見る
</AffiliateLink>
```

このコンポーネントが `rel="sponsored nofollow noopener"` と `[PR]` 表記を自動で付ける。

計測用の1x1画像タグが指定されている案件は、リンクテキストの直後に含める。

---

## 4. 法令・規約

**このセクションの内容は、判断に迷ったら必ず運営者に確認する。エージェントが独自に緩めてはいけない。**

- **ステルスマーケティング規制**(2023年10月施行) — 広告を含む記事には広告である旨の明示が必要。
  `AdDisclosure` バナー(記事冒頭・自動描画)と `AffiliateLink` の `[PR]` 表記で対応している。
- **景品表示法** — 金融商材では「絶対」「必ず得する」などの断定・誇大表現を使わない。
  年会費・還元率・特典の数値は公式サイトで確認した内容のみ記載する。
- **数値の鮮度** — 還元率や年会費は予告なく変更される。記事には必ず
  「申し込み前に公式サイトで最新情報を確認する」旨を含める。
- **ASP規約** — ASPごとに表現ガイドラインとリンクの貼り方の規約が異なる。提携後に必ず確認する。
- **新しいASPと提携したら** `src/site.config.ts` の `asps` 配列に追加する。
  プライバシーポリシーの記載が自動で連動する。

---

## 5. 技術仕様

### 5.1 スタック

Astro 7(静的サイト生成) / Tailwind CSS 4 / MDX / TypeScript

### 5.2 自動生成される要素

**記事を書くときに手作業で用意しなくてよいもの:**

| 要素 | 生成元 |
| :--- | :--- |
| アイキャッチ画像 | `ArticleThumb.astro` — カテゴリー色 + アイコンで生成。画像ファイル不要 |
| OGP画像(SNSサムネイル) | `src/pages/og/[...route].ts` — ビルド時に記事ごとのPNGを生成 |
| 関連記事 | `RelatedArticles.astro` — 同カテゴリーから自動選出、不足分は新着で補完 |
| パンくず | `ArticleLayout.astro` |
| 構造化データ(JSON-LD) | `src/schema.ts` — Article / BreadcrumbList / WebSite+Person |
| 目次 | `TableOfContents.astro` — 見出しから生成 |
| サイトマップ | `@astrojs/sitemap` |
| 記事本数の表示 | トップページで実際の公開記事数を数えている(ハードコードではない) |

### 5.3 触ってはいけない箇所

- **`wrangler.jsonc` を削除しない。**
  このファイルが無いと `wrangler deploy` が「自動構成」フローに入り、`astro add cloudflare` で
  アダプターを勝手に追加したうえでビルドをやり直す。アダプターが入るとページが Workers 環境で
  実行され、**OGP画像の生成が失敗してデプロイが落ちる**(2026-08-30 に実際に発生)。
- **`fonts/NotoSansJP-VF.ttf` を削除しない。**
  OGP画像に日本語を描画するために必要。無いと豆腐文字(□□□)になる。
  ビルド時のみ使用し、読者には配信されない。ライセンスは `fonts/OFL.txt`。
- **`src/content.config.ts` の zod enum を緩めない。**
  カテゴリーのタイプミスをビルド失敗として検出するための仕組み。

### 5.4 開発コマンド

```
astro dev --background     # 開発サーバー起動(バックグラウンド)
astro dev stop / status / logs
npm run build              # 本番ビルド。OGP画像生成を含むため20〜30秒程度かかる
```

ビルド後、`dist/og/*.png` が生成されていることと、`npm run build` が成功することが
デプロイ前の最低確認ライン。

### 5.5 計測

`src/site.config.ts` の `analyticsConfig` に設定済み。

- GA4 測定ID: 設定済み
- Search Console 確認タグ: 設定済み(URLプレフィックス型、HTMLタグ方式で認証済み)
- サイトマップ `sitemap-index.xml` は Search Console に送信済み

値を空文字にすると、対応するタグは一切出力されない。

---

## 6. 運用

### 6.1 記事を追加する手順

1. 2.2 のチェックリストで、そのテーマを書くべきか判断する
2. `src/content/articles/<slug>.mdx` を作成
3. `npm run build` が成功することを確認(カテゴリーの誤りはここで落ちる)
4. コミットして push → Cloudflare が自動でビルド・デプロイ

### 6.2 Search Console の確認サイクル

**2026年9月中旬以降**に検索パフォーマンスを確認する。それまではデータが蓄積されていないため、
既存記事のリライト優先度は判断しない。

見るべきもの: 表示回数はあるが順位が低いクエリ。そこが加筆・リライトの候補になる。

### 6.3 エージェントが代行できない作業

アカウント登録・支払い・本人確認を伴うため、運営者本人の操作が必要。

- ASP(A8.net、AccessTrade等)への登録・案件の提携申請
- Search Console でのインデックス登録リクエスト
- Cloudflare / GitHub のダッシュボード操作(ビルドログの確認を含む)
- ドメイン・各種アカウントの管理

---

## 7. 未解決の課題

| 課題 | 内容 | 対応 |
| :--- | :--- | :--- |
| ASP宣言の不一致 | 記事内で AccessTrade のリンクを使用しているが、`site.config.ts` の `asps` は `["A8.net"]` のみ。プライバシーポリシーに AccessTrade の記載が無い | **要対応**(法令・規約に関わるため運営者の確認のうえ修正) |
| 広告表記の過剰 | `AdDisclosure` が全69記事に描画されるが、実際にアフィリエイトリンクを含むのは13本。残り56本では記載内容が事実と異なる | 要判断(法的リスクは低いが不正確) |
| 記事内の数値の陳腐化 | 年会費・還元率などは自動更新されない。手動でのメンテナンスが必要 | 継続課題 |
| サイト名の重複 | 「カード辞典」(cardjiten.jp)という類似名のサイトが存在。指名検索を育てる段階で混同のリスク | 認識のみ |
| OGP画像の未確認 | SNSでの実際の表示は未検証 | Twitter Card Validator 等で確認 |

---

## 関連ドキュメント

- [README.md](README.md) — プロジェクト構成、記事の書き方の概要
- [DEPLOY.md](DEPLOY.md) — デプロイ手順
- [CLAUDE.md](CLAUDE.md) — エージェント向けの開発ルール
